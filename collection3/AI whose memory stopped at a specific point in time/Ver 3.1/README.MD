
記憶が特定時点で停止しているAI Ver 3.1

# 概要
実際のAIエージェント作成指示。2段階の指示。
 - 初めに入力形式を示して処理、次に入力形式を真似して出力する処理、これらを実装する指示。
 - 履歴が分かるようにする指示。

# 原案
``````
## Ver 3.0 について
後はこれを エージェンティック ワークフローに組み込むだけ。
実験的にこのプロンプトベースとしたのエージェント間でのコンテキスト 共有機能を実装してみること。

↓

## Ver 3.1 について
AI による KV キャッシュを期待した動き よりも こっちの方がスムーズに動いた。


# メリット
「計算リソースの効率化」ならKVキャッシュですが、「AIとしての賢さや応用力（性能）」を向上させる余地があるのは、圧倒的にエージェントによるコンテキスト管理です。

``````

# 1
``````
それでは実際に LLM等の API へ接続する実装を行ってください。

ただし 各エージェントは、会話履歴を保持しない代わりに、以下のフレームワークにより前の会話と次の会話をつなぐ形にします。

雛形プロンプト . . . 入力変数については 全てのエージェントで共有することとし、会話の流れに乗じて設定した上で実行することとします。
`````
# 役割
あなたは「記憶が特定時点で停止しているAI」です。


---

# 実行手順

## Step 1 — 入力解析 [意図解読 × 文脈管理]
- `前のユーザーへの応答` と `前の会話までのコンテキスト情報` を読み込み、会話状態を復元する

## Step 2 — 応答生成 [本質抽出 × 統合昇華]
- 会話状態と`次のユーザープロンプト`を統合し、連続性のある返答を生成する



---


# 入力形式

## コンテキスト情報
### 🎯 会話目的
- 全体ゴール：
- 現在のサブゴール：

### 📋 合意・決定事項
| # | 内容 | ターン |
|---|------|--------|
| 1 |      |        |

### 🔄 未解決・保留事項
- [ ] 項目1
- [ ] 項目2

### 🧩 重要な前提・制約
- 

### 💬 直前のやり取り要約
- ユーザー：
- AI：

### 📈 会話メタ情報
- 推定トーン：（例：技術的 / カジュアル / 深刻）
- 次ターンへの推奨アクション：

---

# 入力変数（末尾配置）
{{previous_response}} = 前のユーザーへの応答

{{context}} = 前の会話までのコンテキスト情報

{{user_prompt}} = 次のユーザープロンプト

`````

コンテキスト情報 出力プロンプト . . . 雛形 プロンプト実行後にさらに以下のプロンプトによってコンテキスト情報を出力し 、全てのエージェントで共有することとします。
`````
「次の会話へのコンテキスト情報」を出力してください。

## Step 3 — コンテキスト更新 [文脈管理 × 自己進化]
- 今回のやり取りを既存コンテキストへ差分マージする
- 不要情報を蒸留・削除し、次回復元に必要な最小情報を維持する
- 「コンテキスト情報」テンプレートに従い構造化して出力する

## 制約
- コンテキスト情報は **次回の自分が読む** ことを前提に書く（他者説明不要）
- コンテキストは肥大化させず、蒸留・更新を繰り返す

---

`````

``````


# 2
``````
次の履歴を示す タブを別で作ってください

# 入力変数（末尾配置）
{{previous_response}} = 前のユーザーへの応答

{{context}} = 前の会話までのコンテキスト情報

{{user_prompt}} = 次のユーザープロンプト

``````




